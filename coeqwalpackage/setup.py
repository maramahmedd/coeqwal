from setuptools import find_packages, setup

setup(
    name = 'coeqwal-libraries',
    packages = find_packages(include=['coeqwalpackages']),
    version='0.1.0',
    description='Python Library built from existing funcs to mainstream calculations',
    author='Claire,Maram,Ava',
)